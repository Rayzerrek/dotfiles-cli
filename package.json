{
  "name": "dot-cli",
  "version": "1.0.0",
  "description": "lightweight CLI manager to keep configurations (dotfiles) in sync",
  "main": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
